spring:
  application:
    name:
      StoreManagement
  server:
    port:
      8080
  datasource:
    url:
      jdbc:postgresql://localhost:5432/StoreManagementDB
    username:
      postgres
    password:
      0468
  jpa:
    hibernate:
      ddl-auto:
        update
    database-platform:
      org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        format_sql: true
    show-sql:
      true
    open-in-view: false