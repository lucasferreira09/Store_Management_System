spring:
  application:
    name:
      StoreManagement
  server:
    port:
      8080
  datasource:
    url:
      jdbc:postgresql://localhost:5432/StoreManagementDB
    username:
      postgres
    password:
      0468
  jpa:
    hibernate:
      ddl-auto:
        none
    database-platform:
      org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate.format_slq:
          true
    show-sql:
      true